NODE_ENV=dev
BROWSER=chromium
HEADLESS=false
TIMEOUT=90000
RETRIES=1
WORKERS=1
# BASE_URL and LOGIN_URL will be dynamically resolved based on CLIENT
# USERNAME and PASSWORD will be dynamically resolved based on CLIENT
TRACE=retain-on-failure
VIDEO=retain-on-failure
SCREENSHOT=only-on-failure
CLIENT=demo
