---
description: >
  DB state verification SQL scripts. Generates SQL queries to verify database state before and
  after test execution, checking row counts, field values, and data integrity constraints.
  Used alongside Playwright tests to validate end-to-end data persistence.
  Keywords: database verification, DB state, SQL verification scripts, data integrity,
  row counts, before after test, DB assertions.
argument-hint: <table-name | stored-procedure>
---

@.github/skills/database-verification/SKILL.md

$ARGUMENTS
